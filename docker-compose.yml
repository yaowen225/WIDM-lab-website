version: '3.8'

services:
  widm-dashboard-end:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    container_name: widm-dashboard-end
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=https://widm-back-end.nevercareu.space
      - VITE_CLIENT_ID=20240618161634J1sGMZdQrgsj
      - VITE_REDIRECT_URI=https://widm-back-end.nevercareu.space/auth/return-t

  widm-front-end:
    build:
      context: ./front-end
      dockerfile: Dockerfile
    container_name: widm-front-end
    ports:
      - "7230:7230"
      - "24678:24678"
    environment:
      - API_URL=https://widm-back-end.nevercareu.space
